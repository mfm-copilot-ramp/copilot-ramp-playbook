<?xml version="1.0" encoding="UTF-8"?>
<!-- Overrides the MkDocs default sitemap to add changefreq + priority hints. -->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{%- for file in pages -%}
    {% if not file.page.is_link and (file.page.abs_url or file.page.canonical_url) %}
    <url>
         <loc>{% if file.page.canonical_url %}{{ file.page.canonical_url|e }}{% else %}{{ file.page.abs_url|e }}{% endif %}</loc>
         {% if file.page.update_date %}<lastmod>{{ file.page.update_date }}</lastmod>{% endif %}
         <changefreq>{% if file.page.is_homepage %}weekly{% else %}monthly{% endif %}</changefreq>
         <priority>{% if file.page.is_homepage %}1.0{% else %}0.7{% endif %}</priority>
    </url>
    {%- endif -%}
{% endfor %}
</urlset>
