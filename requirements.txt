# Build dependencies for the Copilot Ramp Playbook (MkDocs Material).
# Pinned so local builds and the GitHub Pages CI build match exactly.
# Install locally with:  pip install -r requirements.txt

mkdocs-material==9.5.39
mkdocs-rss-plugin==1.19.0
mkdocs-git-revision-date-localized-plugin==1.5.3

# Social share card generation — Material's "social" plugin (the mkdocs-material[imaging]
# extra). Pinned within the ranges declared for mkdocs-material 9.5.39 (cairosvg~=2.6,
# pillow~=10.2). Needs system Cairo/FreeType libraries on the build host: installed in the
# CI/deploy workflows, and documented for local builds in GO-PUBLIC.md.
cairosvg==2.9.0
pillow==10.4.0
